# Turso 备份配置（用于把本地 data.db 备份到 Turso）
# 复制本文件为 .env.local，并填入真实值（.env.local 已被 .gitignore 忽略，不会提交）
#
# 重要：请用 TURSO_BACKUP_* 而不是 TURSO_DATABASE_URL / TURSO_AUTH_TOKEN。
# 后者会让应用直接连远程库（lib/db.ts 会切到 Turso），本地 data.db 就不再被读写了。
#
# 数据库地址（Turso 后台，形如 libsql://<db-name>-<org>.aws-<region>.turso.io）
TURSO_BACKUP_URL=libsql://<your-db-name>-<your-org>.aws-us-east-2.turso.io
# 数据库 auth token（Turso 后台生成，轮换后填入新的）
TURSO_BACKUP_TOKEN=your-new-token-here

# 可选：直连不通时，用本地代理访问 Turso（如 Clash/SS 的 HTTP 端口）
# TURSO_BACKUP_PROXY=http://127.0.0.1:7890
